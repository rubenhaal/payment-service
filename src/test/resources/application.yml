spring:
  cloud:
    config:
      enabled: false

order-service:
  welcome-message: "Mensaje de prueba"
  environment: "test"
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics